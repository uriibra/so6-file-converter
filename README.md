>> so6_to_df --> read raw file
>> so6_to_df_simple --> groups by callsign and changing variables will be saved into a list
>> so6_to_df_waypoints --> reads the file raw, dividing segments into single lines, deletes duplicates
>> route --> route of so6 file
